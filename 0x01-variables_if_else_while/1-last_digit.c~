#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
*main - Main function of the project
*Return: return 0 all the time
*/
int main(void)
{
  int n;
  srand(time(0));
  n = rand() - RAND_MAX / 2;

  return (0);
}
