#include"main.h"
#include <stdio.h>
/**
*_abs - absolute value
*@n: The int ot print
*main - Main function of the project
*Return: return 0 all the time
*/
int _abs(int n)
{
if (n < 0)
{
return (n * (-1));
}
else if (n==0)
{
return (0);
}
else
{
return (n);
}
}
